@model IEnumerable<ProjetoEstoque.Models.CompraVenda>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ValorFornecedor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValorCliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QuantidadeComprada)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QuantidadeVendida)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ValorFornecedor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValorCliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QuantidadeComprada)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QuantidadeVendida)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NomeProdutoC }) |
            @Html.ActionLink("Details", "Details", new { id=item.NomeProdutoC }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NomeProdutoC })
        </td>
    </tr>
}

</table>
